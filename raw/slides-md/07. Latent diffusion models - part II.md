# **Generative Artificial Intelligence for graphics and multimedia** Latent diffusion models – part II Prof. Lia Morra 

## **Recap: latent denoising model** 

Rombach et al. "High-resolution image synthesis with latent diffusion models." _CVPR_ 2022. 

## **Focusing on sampling** Can we make sampling faster and Generation with diffusion model is a slow iterative process. more efficient 

Can we steer generation during sampling (without retraining the LDM) 

From score-models to efficient sampling 

## **Defining the score** 

- For a density 𝑝 x on ℝ[D] , the score function is the gradient of the logdensity: 

   - 𝑠( x ) ≔∇𝑥 log 𝑝 x , 𝑠: ℝ[D] →ℝ[D] 

- **Intuition** : the score forms a **vector field** that points toward regions of higher probability, providing a local guide to where the data is most likely to occur 

Lai, Chieh-Hsin, et al. "The principles of diffusion models." Chapter 3 _arXiv preprint arXiv:2510.21890_ (2025). 

## **Why the score?** 

𝑝෤ x Given x = 𝑝 𝑍 the score is independent of the (untractable) normalization constant Z 𝑠( x ) ≔∇𝑥 log 𝑝 x = ∇𝑥 log ෤𝑝 x −∇𝑥 𝑍 

**==> picture [35 x 19] intentionally omitted <==**

The score function 𝑠( x ) ≔∇𝑥 log 𝑝data x is approximated directly from 𝑠 x samples of 𝑝data, parameterized by a neural network ( ) 𝜙 

## **Score matching** 

**Training** : aligns the model score with the (unknown) data score 1 2 ℒ 𝑠 x x SM 𝜙= ( ) −𝑠( ) 𝜙 2[𝔼][x][ ~𝑝][data] 2 x 𝑠( ) is generally unknown but the loss can be reformulated 𝑠 to depend only on and the data samples 𝜙 

**Inference** : uses the trained score model using Langevin dynamics equation ∗ x = x x with 𝝐 ~𝒩 𝑛+1 𝑛 + 𝜂𝑠 ( 𝑛 )+ 2𝜂𝝐𝑛, 𝑛 𝟎, 𝑰, 𝑛= 0, 1, 2 … 𝜙 

## **Sampling in score-based models** 

Langevin sampling using the score function ∇𝑥 log 𝑝 x to guide trajectories toward highdensity regions via the update equation 

Lai, Chieh-Hsin, et al. "The principles of diffusion models." Chapter 3 _arXiv preprint arXiv:2510.21890_ (2025). 

## **Denoising score matching: the conditioning trick** 

𝐱 𝐱 Recall: in DDPM, instead of estimating q ( t−𝟏 |) t 𝔼 𝐷 𝐱 𝐱 𝐱 𝐱 pt col 𝐱t KL €C q t−𝟏 1) t ฮ𝑝𝜙 € t−𝟏 1 t YY’ we make it tractable by conditioning on a clean data sample 𝑞 ( 𝐱t−𝟏 | 𝐱t, 𝐱𝟎 

The neural network 𝑠 (෤𝐱; 𝜎) is trained to approximate the score of the 𝜙 perturbed (noisy) data 

෤𝐱= 𝐱𝑑𝐱 𝑝𝜎 p𝜎 ෤𝐱|𝐱𝑝data Oo න ~~€) ©~~ **Noise transition function real data** 

## **Conditioning trick in action** EASY: 

Source https://the-principles-of-diffusion-models.github.io/#/blog 

## **Denoising score matching** 

**Training** : aligns the model score with the (unknown) data score 1 2 ℒ 𝑠 DSM 𝜙, 𝜎= (෤𝐱; 𝜎) −∇෤𝐱 log 𝑝 ෤𝐱|𝐱 𝜙 Cc) ~~-~~ 2[𝔼][෤𝐱~𝑝][𝜎] |i C ll 2 | Special case: Gaussian Noise 1 𝛜 2 ℒ 𝑠 DSM 𝜙, 𝜎= (𝐱+ 𝜎𝛜; 𝜎) + 𝜙 𝒩 𝟎, 𝜎[2] 𝐈 2[𝔼][𝐱,𝛜] 𝜎 2 **Inference** : uses the trained score model using Langevin dynamics equation ∗ = ෤𝐱 ෤𝐱 with 𝝐 ~𝒩 𝑛+1 𝑛 + 𝜂𝑠𝜙 (෤𝐱𝑛 ; σ)+ a 2𝜂𝝐𝑛, 𝑛 ( 𝟎, 𝑰, 𝑛= 0, 1, 2 … ) With sufficiently small σ and large 𝑛, **x** 𝑛 approximates samples from 𝑝data 

## **Multi-Noise Levels of Denoising Score Matching (NCSN)** 

- In score-matching models, the noise variance 𝜎 is fixed 

- low variance → vanishing gradients in low-density regions → sampling multimodal distribution is harder 

- high variance → model captures only coarse structures → blurry samples that lack fine detail 

## **Multi-Noise Levels of Denoising Score Matching (NCSN)** 

In NCSN models, the noise variance 𝜎 is varied according to a schedule 

- 0 < 𝜎 < 𝜎 < ⋯< 𝜎 

- during training we increase it 1 2 𝐿 

- during sampling we revert it 

**==> picture [808 x 113] intentionally omitted <==**

**----- Start of picture text -----**<br>
p𝜎𝑖 𝐱𝜎𝑖|𝐱<br>𝐱 𝐱 … 𝐱 𝐱 … 𝐱<br>𝟎 1 l−1 l 𝐿<br>**----- End of picture text -----**<br>


## **Multi-Noise Levels of Denoising Score Matching (NCSN)** 

**Training** : aligns the model score with the (unknown) data score by minimizing the DSM objective across all noise levels 

L ℒNCSN 𝜙≔σi=1 λ(σ𝑖) ℒDSM 𝜙, σ𝑖 

## with 

**==> picture [522 x 71] intentionally omitted <==**

**Inference** : generates data by progressively denoising from a high noise level 𝜎 down to a low noise level 𝜎 ≈0 𝐿 1 → at each step, applies sampling: 2 ∗ 𝜎ℓ = ෤𝐱𝑛+1 ෤𝐱𝑛 + 𝜂ℓ 𝑠 (෤𝐱𝑛 ; σl)+ 2𝜂ℓ𝝐𝑛, with 𝝐𝑛~𝒩 𝟎, 𝑰, 𝜂ℓ = 𝛿⋅[with 𝛿> 0] 𝜙 𝜎 1[2] 

## **NCSN: an example** 

## Annealed Langevin dynamics for the NCSN model 

Source: https://yang-song.net/blog/2021/score/ 

## **DDPM vs. NCSN** 

**==> picture [857 x 260] intentionally omitted <==**

**----- Start of picture text -----**<br>
NCSN DDPM<br>𝐱𝐢+𝟏 ∣𝐱𝐢 2 2 𝐱𝐢+𝟏 = 1 −𝛽𝑖𝐱𝐢 + 𝛽𝑖𝝐<br>𝐱 = 𝐱 + 𝜎 −𝜎 𝝐<br>𝐢+𝟏 𝐢 𝑖+1 𝑖<br>2<br>𝑝 rior 𝒩 𝟎, 𝜎𝐿 𝐈 𝒩 𝟎, 𝐈<br>p<br>2 2<br>Loss 𝝐 𝔼 𝔼 𝛜<br>𝔼i𝔼𝐱~𝑝data 𝔼𝝐~𝒩(𝟎,𝑰) 𝑠𝜙(𝐱𝐢; σi) − i 𝐱~𝑝data 𝔼𝝐~𝒩(𝟎,𝑰) 𝜙(𝐱𝐢; i) −𝛜 2<br>σ [2]<br>2<br>Sampling Apply Langevin per layer; use  Traverse the chain with<br>∗ 𝐱 𝐱<br>output to initialize the next 𝑝 𝐢−𝟏 𝐢<br>𝜙<br>**----- End of picture text -----**<br>


## **From discrete to continuous equation** 

𝐱 →𝐱 Both DDPM and NCSN can be reformulated as time progressions i+1 𝑡+Δ𝑡 

## **NCSN** 

**DDPM** 

2 𝑑𝜎 2 2 𝑡 𝐱𝑡+Δ𝑡 = 𝐱𝑡 + 𝜎𝑡+Δ𝑡 −𝜎𝑡 𝛜𝑡 ≈𝐱t + 𝑑𝑡[Δ𝑡𝝐][𝑡] 1 𝐱𝑡+Δ𝑡 = 1 −𝛽tΔ𝑡𝐱t + 𝛽tΔ𝑡𝝐𝑡 ≈𝐱t − 𝛽Δ𝑡𝝐𝑡 2[𝛽][t][𝐱][t][Δ𝑡+] 

**General form** 

𝐱 ≈𝐱 + Δ𝑡𝝐 𝑡+Δ𝑡 𝑡 𝐟(𝐱𝑡, 𝑡) Δ𝑡+ 𝑔(𝑡) 𝑡 

## **From discrete to continuous equation** 

**General form** 

𝐱 ≈𝐱 + Δ𝑡𝝐 𝑡+Δ𝑡 𝑡 𝐟(𝐱𝑡, 𝑡) Δ𝑡+ 𝑔(𝑡) 𝑡 

**Stochastic differential equation (SDE)** 

d𝐱(𝑡) = 𝐱𝑡 + 𝐟(𝐱(𝑡), 𝑡)d𝑡+ 𝑔(𝑡)d𝐰(𝑡) →ℝ[D] 𝒇 ⋅, 𝒕: ℝ[D] **Drift function** 

**Diffusion (Brownian) process** 

# **SDE as distribution matching** 

## **Forward SDE** 

## 𝐱 d𝐱(𝑡) = 𝑡 + 𝐟(𝐱(𝑡), 𝑡)d𝑡+ 𝑔(𝑡)d𝐰(𝑡), 

## 𝐱(0) ~𝑝data 

, 

Source: https://yang-song.net/blog/2021/score/ 

## **Reverse SDE** 

## 𝑑ത𝐱 𝑡= 𝑓 ത𝐱 𝑡, 𝑡−𝑔[2] 𝑡, ∇𝑥 log 𝑝𝑡 ത𝐱 𝑡 𝑑𝑡+ 𝑔 𝑡𝑑ഥ𝐰 𝑡, 

## ത𝐱 ≈𝑝𝑇 𝑇∼𝑝prior 

Source: https://yang-song.net/blog/2021/score/ 

## **Reverse SDE** 

## At inference time, sampling is equivalent to solving the reverse SDE (numerical problem) 

𝑑ത𝐱 𝑡= 𝑓 ത𝐱 𝑡, 𝑡−𝑔[2] 𝑡∇𝐱 log 𝑝𝑡 ത𝐱 𝑡 𝑑𝑡+ 𝑔 𝑡𝑑ഥ𝐰 𝑡, 

ത𝐱 ≈𝑝𝑇 𝑇∼𝑝prior 

Source: https://yang-song.net/blog/2021/score/ 

## **From SDEs to Flow Matching** 

- Score SDEs and CNFs learn a continuous-time flow, either stochastic or deterministic, . 

- from a simple Gaussian prior sample 𝝐~ 𝑝prior to a data-like sample ~𝑝data 

- **Flow Matching (FM)** generalizes it to learn a continuous flow between two arbitrary fixed endpoint distributions: a **source distribution** 𝑝src and a **target distribution** 𝑝tgt, both assumed to be easy to sample from. The flow is governed by the velocity: 

d𝐱(𝑡)[𝐯][t] 𝐱 𝑡 d𝑡[=] 

- In the case of a Gaussian prior 

## d𝐱(𝑡)(𝑡)𝑡)) 

d𝐱(𝑡)(𝑡)𝑡)) 1[𝐯][t] 𝐱 𝑡 = 𝑓(𝑡) 𝐱(𝑡) − x (𝑡) d𝑡[=] 2[𝑔][2][(𝑡) ∇][𝑥][log 𝑝][t] In practice, we learn a conditional velocity conditioning on a latent variable 𝐯t 𝐱 𝑡| 𝐳 

Lipman, Yaron, et al. "Flow matching for generative modeling." _arXiv preprint arXiv:2210.02747_ (2022). 

## **Flow matching** 

**Training** : approximate the oracle velocity field 𝐯𝑡 using a neural network 𝐯𝜙, by minimizing the expected squared error 

1 2 ℒ 𝐯 x x FM 𝜙= ( 𝑡, 𝑡) −𝐯𝑡( 𝑡) 𝜙 2[𝔼][𝑡,][x][𝑡][~𝑝][𝑡] 2 Conditioning 1 2 trick ℒ 𝐯 x x z FM 𝜙= ( 𝑡, 𝑡) −𝐯𝑡( 𝑡| ) 𝜙 2[𝔼][𝑡,][z][~π(][z][),][x][𝑡][~𝑝][𝑡][(⋅|][z)] 2 **Inference** : computes the flow map to integrate the velocity from the initial noise to the target distribution 

**diff objective same optimum** 

## **Rectified flow** 

**Noise scheduler (flow trajectory)** follows a linear function: x 𝑡 = 1 −𝑡 x 0 + 𝑡 **ϵ** 

**Conditional velocity** is trained across all data-noise pairs ( **ϵ** , x 0) that could produce **x** at time _t_ 

− 𝐯 ( x, 𝑡) =𝔼 **ϵ** x 0 | x 𝑡 = x 𝜙 

## **Why does this matter for sampling?** 

x : Every SDE has a deterministic ODE with the same margins 𝑝t 1 d𝒙= 𝑓(𝐱(𝑡), t) − x (𝑡) d𝑡 2[𝑔][2][(𝑡) ∇][𝑥][log 𝑝][t] | ~~7~~ ( ] Data (xo) Forward Reverse To generate samples: 

- 𝐱 

- draw an initial sample T ~ 𝑝prior 

- numerically solve (integrate) the ODE backward in time from 𝑡= T to 𝑡= 0. 

- Unlike SDE, ODE is directly invertible and enables exact log-likelihood computation. 

## **How to make sampling faster?** 

Original DDPM: T = 1000 steps → 20–60 s per image on consumer GPUs. Unacceptable for real-world, iterative applications. 

**Numerical sampling** Same weights, different numerical ODE 

- → DDIM, DPM-Solver++ 

**Learn to sample: distillation** Faster Student distilled by the teacher → Progressive Distillation 

- **1-Step Learning** 

in Model that maps t → x₀ one step → Consistency Models 

**20–50 steps** 

**1–4 steps** 

## **DDIM sampling** 

**Key idea** : reinterpreting the diffusion process as deterministic ODE instead of stochastic SDE. Apply numerical techniques to reduce the number of steps required. 

## 𝑠 _→_ 𝑡 **: Update step** 

**==> picture [693 x 156] intentionally omitted <==**

**----- Start of picture text -----**<br>
𝛼 𝛼<br>𝑡 𝑡<br>𝐱𝑡 = 𝐱𝑠 + 𝜎𝑡2 −𝑐𝑡,𝑠2 − 𝜎𝑠 𝜖∗𝜙 𝐱𝑠, 𝑠+ 𝑐𝑡,𝑠𝜖𝑡, 𝜖𝑡 ∼𝒩 0, 𝐼<br>𝛼 𝛼<br>𝑠 𝑠<br>=<br>Xs if 𝑐𝑡,𝑠 0<br>a,x" (X,,<br>→ deterministic<br>5) + aus)<br>Discretization PF-ODE<br>Error Oracle Trajectory sampling<br>**----- End of picture text -----**<br>


## **Sam lin efficienc p g y** 

|**Solver**|**Intuition**|**Number**<br>**steps**|**Additional applications**|
|---|---|---|---|
|DDPM|Standard sampling|1000||
|DDIM|The reverse ODE can be<br>solved using Euler method.<br>The discretization step<br>regulates the quality|50 - 100|**DDIM Inversion:**ODE is reversible:<br>you can encode a real image in<br>the latent space and then edit it<br>while maintaining structure and<br>identity.|
|DPM-<br>Solver|The reverse ODE has a<br>semi-linear structure that<br>can be exploited<br>analytically. Order 2 and 3<br>methods converge much<br>faster (such as Runge-Kutta<br>vs Euler)|10 - 20|**Classifier guidance:**DPM-<br>Solver++ and DPM-Solverv3 are<br>designed to handle classifier free<br>guidance (CFG)|



## **Consistency models** 

Progressively distill a step T-model into a T/2 model, iteratively. 

**Consistency Distillation:** distilling from an existing teacher model **Consistency Training:** training directly without a teacher (slower but independent) **Result:** sampling in 1–2 steps — the practical limit to real-time diffusion 

Progressive Distillation for Fast Sampling of Diffusion Models. T. Salimans, J. Ho. 2022. 

## **Recap** 

_All the generative frameworks seen are instances of a conceptual continuum — the score is the glue._ 

**NF (2015–18) Score Matching (2019) SDE (2020) DDIM (2021) Flow Matching (2022) Consistency (2023)** 

Exact likelihood via invertibility. CNF as a continuous limit. Score s(x) = ∇x log p(x) — avoid normalization constant. DDPM ≡ VP-SDE, NCSN ≡ VE-SDE. Each SDE has equivalent ODE. Probability Flow ODE → deterministic and invertible sampling. Direct vector field regression — no ODE simulation. Distillation towards 1-step — the goal of real-time diffusion. 

# Flow matching in practice 

**Stable Diffusion 3** Esser, Patrick, et al. "Scaling rectified flow transformers for high-resolution image synthesis." _ICML_ . 2024. 

## **Stable Diffusion 3  = Rectified flow + DiT** 

## Replace U-net with transformer-based encoder-decoder 

Peebles, William, and Saining Xie. "Scalable diffusion models with transformers.“CVPR. 2023. 

## **Stable diffusion 3  = Rectified flow + DiT** 

Replace U-net with transformer-based encoder-decoder →  better ability to scale, more parameters, higher quality Multi-modal architecture (text + images), improved text encoding →  better ability to follow prompt 

Replace DDPM with Rectified Flows 

→  improved training stability, tailored and faster sampling (10-30 steps vs. 50-1000) 

smiling cartoon dog sits at a table, coffee mug on hand, as a room goes up in flames. “This is fine,” the dog assures himself. 

A whimsical and creative image depicting a hybrid creature that is a mix of a waffle and a hippopotamus. This imaginative creature features the distinctive, bulky body of a hippo, but with a texture and appearance resembling a golden-brown, crispy waffle. The creature might have elements like waffle squares across its skin and a syrup-like sheen. It’s set in a surreal environment that playfully combines a natural water habitat of a hippo with elements of a breakfast table setting, possibly including oversized utensils or plates in the background. The image should evoke a sense of playful absurdity and culinary fantasy 

A yellow and blue A stop sign that has bus drives down some papers on it. the road. 

a photo of a photo of fire suitcase and hydrant and potted bear; bear is plant; potted plant below suitcase is above fire hydrant 

A raccoon wearing formal clothes, wearing a tophat and holding a cane. The raccoon is holding a garbage bag. Oil painting in the style of Hokusai. 

## **SD1.5** 

## **SD2.1** 

**SD3.0** 

Courtesy: Pietro Basci 

# Conditioning and guidance 

## **Conditional generation** 

In practice, we are interested in conditional generation 𝑝𝑡(𝐱|𝐜), with 𝐜~𝑝(𝐜) a conditioning variable such as a class label, caption, or other auxiliary information. 

**Q1** : how can we control the consistency between the generated images and the conditioning signal? **Q2:** can we steer a pre-trained diffusion model? 

## **Classifier guidance** 

A first approach used an **external classifier** 𝑝𝑡 ( x 𝑡|𝐜 ) to guide sampling. 

The conditional score can be expressed as: 

𝑝𝑡 𝐜 | x 𝑡𝑝𝑡 x 𝑡 ∇ x = ∇ x log 𝑝𝑡 𝑡|𝐜 x log 𝑡 Co 𝑡 ~~Fea~~ 𝑝𝑡 𝐜 ∇ x =∇ x + ∇ x −∇ 𝐜 x log 𝑝𝑡 𝑡|𝐜 x log 𝑝𝑡 𝑡 x log 𝑝𝑡 𝐜| 𝑡 x log 𝑝𝑡 𝑡 ( ) 𝑡 ( ) 𝑡 () 2.) 𝑡 =∇ x log 𝑝𝑡 ( x 𝑡 ) + ∇ x log 𝑝𝑡 ( 𝐜| x 𝑡 ) 𝑡 𝑡 unconditional score classifier gradient 

## **Classifier guidance** 

**Training** : CG is a training-free method (w.r.t. the diffusion model), but requires an **external c** classifier trained separately to predict the condition from noisy x . It must be on _t_ inputs 𝑝𝑡 𝑡|𝐜 **explicitly conditioned** (e.g., via time embeddings), since it is expected to operate reliably across all noise levels. 

- **Inference** : at inference time, the classifier gradient is added to the ω 

- unconditional score function and scaled by a guidance weight ∗ 

- ∇ x log 𝑝𝑡 x 𝑡|𝐜 =𝑠 ( x 𝑡, 𝑡)+ω ∇ x log 𝑝𝑡 𝐜| x 𝑡, 𝑡 𝑡 𝜙 𝑡 

- 𝜔= 1 → recovers the true conditional score 

- 𝜔> 1 → amplifies the classifier signal (fidelity ↑ diversity ↓) 

- 0 ≤𝜔< 1 → down-weights the classifier signal (diversity ↑ fidelity ↓) 

## **Issues with classifier guidance** 

- The classifier must be trained to operate across all noise levels, which is computationally expensive 

- Classifiers must generalize well to severely corrupted inputs 

- Since the classifier is trained independently of the diffusion model, it may not align perfectly with the learned data distribution 

## **Classifier-free guidance (CFG)** 

The key idea is to modify the gradient of the score function in a way that allows for effective conditioning without explicit classifiers. 

- ∇ x =∇ x x x log 𝑝𝑡 𝐜| 𝑡 x log 𝑝𝑡 𝑡 |𝐜−∇ x log 𝑝𝑡 𝑡 𝑡 𝑡 𝑡 

conditional score 

unconditional score (𝐜= ∅) 

To avoid training two separate models for the conditional and unconditional score functions, CFG adopts a **single model** that learns **both score functions** 𝐜 within a single model by treating as an additional input. 

## **Classifier-free guidance (CFG)** 

**c** with **Training** : These two training regimes are unified by randomly replacing the null input ∅ with probability 𝑝uncond (a user-defined hyperparameter typically set to 0.1). 

**Inference** : at inference time, the classifier gradient is added to the ω unconditional score function and scaled by a guidance weight 

   - ∗ ∗ 

   - ∇ x log 𝑝𝑡 x 𝑡|𝐜≈ω 𝑠 ( x 𝑡, 𝑡, c )+(1 −ω) 𝑠 ( x 𝑡, 𝑡, ∅) 𝑡 𝜙 𝜙 

- 𝜔= 1 → recovers the conditional score 

- 𝜔= 0 → recovers the unconditional score 

- 0 < 𝜔< 1 → progressively strengthen guidance (fidelity ↑ diversity ↓) 

## **Why does CFG improves fidelity?** 

- When training a diffusion model conditioned on a signal 𝑦(e.g. a text . 

- caption), the model learns 𝑝θ(𝐱| 𝐜) 

- the model does not learn "which image corresponds to 𝐜" — it learns the **entire distribution** 𝐜. of images compatible with 

- During training, the model minimises a reconstruction loss (MSE on the predicted noise). This implicitly pushes it toward the **expected value** of the distribution: the **mean** of many different images is a **blurry and generic** 

- image, because the details that vary across samples cancel each other out. 

- **Mode-seeking** (what CFG does) concentrates on the peaks of the distribution, producing sharp samples that are faithful to the condition. 

## **Spatial conditioning: ControlNet** 

## **Key idea:** keep the diffusion model fixed, learn how to condition 

**==> picture [724 x 265] intentionally omitted <==**

**----- Start of picture text -----**<br>
Ra eH | a Carat ae \. ont tee<br>~~ mn f ra ra<br>Spatial<br>condition eg . Sa | bh<br>tei ‘NS i \ ] S « . q F fe Li<br>Wiggers \ N Saag PP Ire:<br>accra: Lox me en<br>fpotCamry edge Default | “masterpiece of [fairy] [tale,] [giant] [deer,] [golden] [antlers’] —*..., quaint city Galic”<br>Prompt<br>ag | 7 I L. i © ¥ 5 i \ F ab sitet<br>Spatial<br>condition<br>= J = i si i Fr<br>ae ee SIF 4s<br>npul human pose Default “chef in kitchen” “Lincoln statue”<br>Prompt<br>**----- End of picture text -----**<br>


Zhang, Lvmin, Anyi Rao, and Maneesh Agrawala. "Adding conditional control to text-to-image diffusion models." _CVPR_ . 2023. 

## **ControlNet** 

- For each pre-trained neural block, lock (freeze) the parameters Θ of the original block and simultaneously clone the block to a trainable copy with parameters Θ𝑐 

- The trainable copy takes an external 𝐜 

- conditioning vector as input – not original present in the pre-trained model. 

Zhang, Lvmin, Anyi Rao, and Maneesh Agrawala. "Adding conditional control to text-to-image diffusion models." 

_CVPR_ . 2023. 

## **ControlNet** 

• To add ControlNet to Stable Diffusion, each input conditioning image (e.g., edge, pose, depth, etc.) is converted from an input size of 512 × 512 into a 64 × 64 feature space vector that matches the size of Stable Diffusion. 

- A tiny network ℰ(·) encodes an image𝐜 

- space condition iinto a feature space conditioning vector 𝐜f as, 𝐜f = ℰ(𝐜i). 

Zhang, Lvmin, Anyi Rao, and Maneesh Agrawala. "Adding conditional control to text-to-image diffusion models." _CVPR_ . 2023. 

## **ControlNet** 

- The trainable copy is connected to the locked model with zero convolution layers. 

- By initializing the weights to zero, the output at the start of training is identical to the original model - the pretrained features are preserved from noise. 

- The trainable copy is fully functional and retains the capabilities of the large, pretrained model allowing it to serve as a strong backbone for further learning - control is gained gradually. 

Zhang, Lvmin, Anyi Rao, and Maneesh Agrawala. "Adding conditional control to text-to-image diffusion models." _CVPR_ . 2023. 

## **Which controls?** 

Zhang, Lvmin, Anyi Rao, and Maneesh Agrawala. "Adding conditional control to text-to-image diffusion models." _CVPR_ . 2023. Zhang, Lvmin, Anyi Rao, and Maneesh Agrawala. "Adding conditional control to text-to-image diffusion models." _CVPR_ . 2023. 

## **Alternatives to ControlNet** 

## **IP-Adapter** 

- Decoupled cross-attention for image prompt. Allows you to use a reference image as a style condition while text controls layout and content. 

## **T2I-Adapter** 

- Features extracted from the control signal and injected into the U-Net encoder. Fewer ControlNet parameters, faster training. 

## **ControlNet** 

- ControlNet: Precise control, more parameters, more training time. 

- Adapters: quick to train, combinable with each other, less expressive. 

# More on diversity and fidelity 

## **Trade-offs in generative image models** 

Generative image models should: 

- generate **high quality** /realistic samples 

- be **consistent** with input conditioning 

- produce **diverse** samples 

## **Trade-offs in generative image models** 

Generative image models should: 

- generate **high quality** /realistic samples 

- be **consistent** with input conditioning 

- produce **diverse** samples 

Do SOTA models favor realism and consistency over diversity? Which are the trade-offs to consider? 

## **Pareto fronts in generative image models** 

Text-to-image generation is a **multi-objective** problem: there might not be an optimal overall solution 

## **Pareto fronts in generative image models** 

Astolfi, Pietro, et al. "Consistency-diversity-realism Pareto fronts of conditional image generative models." _arXiv preprint arXiv:2406.10429_ (2024). 

## **Pareto fronts in generative image models** 

Realism and consistency can be improved simultaneously There is a clear **trade-off** between realism/consistency and diversity 

Astolfi, Pietro, et al. "Consistency-diversity-realism Pareto fronts of conditional image generative 

models." _arXiv preprint arXiv:2406.10429_ (2024). 

## **Consistency, diversity, realism** 

Astolfi, Pietro, et al. "Consistency-diversityrealism Pareto fronts of conditional image generative models." _arXiv preprint arXiv:2406.10429_ (2024). 

# Fine-tuning and personalizing models 

## **LoRA (Low Rank) adaptation** 

**Key Idea:** Instead of fine-tuning all model parameters, LoRA freezes the pretrained weights and injects trainable **low-rank decomposition matrices** into each layer of the Transformer architecture. 𝑊 ∈ℝ[𝑑×𝑘] For a pre-trained weight matrix 0 , the update is constrained to: 𝑊0 + Δ𝑊= 𝑊0 + 𝐵𝐴 where 𝐵∈ℝ[𝑑×𝑟] , 𝐴∈ℝ[𝑟×𝑘] and rank 𝑟≪min(𝑑, 𝑘) Only **A** and **B** are trained — 𝑊0 stays frozen. 

## **Why LoRA?** 

- Drastically reduces the number of **trainable parameters** (down to ~0.1–1%) 

- No added **inference latency** (matrices can be merged back) 

- Enables fine-tuning of **billion-parameter models** on consumer hardware 

- Easily **swappable adapters** for different tasks/domains 

## **LoRA in Stable Diffusion** 

LoRA adapters are injected into the **attention layers** of both the **U-Net** (denoising network) and optionally the **text encoder (CLIP).** 

**LoRA Tar et g** 

U-Net onl y 

Text encoder onl y 

U-Net + Text encoder 

**What it ca tures p** Visual style, textures, lighting New conce t-token associations p Characters, specific subjects, artistic st les y 

## **Training setup (typical)** 

- **Dataset:** as few as **10–50 images** for a concept/style 

- • **Rank:** 𝑟∈ 4, 8, 16, 32 —higher 𝑟= more capacity 𝛼 

- • ~~—~~ **Alpha scaling:** output is weighted by 𝑟[ ,][so the effective update is: ] 

- 𝛼 ~~—~~ 

- Δ𝑊= 𝑟[⋅𝐵𝐴] 

- • **Inference:** LoRA weights merged at a chosen **strength** 𝑠∈ 0 1 : 𝛼 ~~—~~ 

- 𝑊[′] = 𝑊0 + 𝑠⋅ 𝑟[⋅𝐵𝐴] 

## **DreamBooth** 

**Key Idea:** Fine-tune the **entire diffusion model** (U-Net + text encoder) to bind a specific subject to a **rare token identifier** (e.g. `[V]` ), so the model can generate that subject in arbitrary contexts. 

Source: https://dreambooth.github.io/ 

## **How does it work?** 

**Reconstruction loss** on subject images 

**==> picture [191 x 26] intentionally omitted <==**

**----- Start of picture text -----**<br>
ℒ= ℒ + ℒ<br>subj prior<br>**----- End of picture text -----**<br>


**Prior preservation loss** (to avoid language drift & overfitting) 

## **Textual inversion** 

**Key Idea:** Keep the **entire diffusion model frozen** and learn a new **text** 𝑆[∗] **embedding vector** for a special token , optimizing it to represent a new concept from a few images. 

## **Comparison at a glance** 

**DreamBooth Textual Inversion LoRA** 

**Pro ert DreamBooth Textual Inversion LoRA p y** Model weights Full None Partial modified File size ~2–7 GB ~5–10 KB ~2–150 MB f f f f f f f f S S S < S § § § Subject fidelity KK OK xX KR WwW Bm ha f f f f , f f f § § § < § § § § Composability Compute High Low Low re uired q 

## **Bibliography** 

https://the-principles-of-diffusion-models.github.io/ See chapters 1, 2, 8, 9 Neurips 2023 Tutorial Latent diffusion models: is the generative AI revolution happening in latent space? Denoising diffusion probabilistic models https://arxiv.org/abs/2006.11239 High-Resolution Image Synthesis with Latent Diffusion Models https://arxiv.org/abs/2112.10752 Emu: Enhancing Image Generation Models Using Photogenic Needles in a Haystack https://arxiv.org/abs/2309.15807 Classifier-Free Diffusion Guidance https://arxiv.org/abs/2207.12598 Adding Conditional Control to Text-to-Image Diffusion Models https://arxiv.org/abs/2302.05543 

