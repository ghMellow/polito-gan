# **Generative Artificial Intelligence for graphics and multimedia** Latent diffusion models Prof. Lia Morra 

# using photogenic needles in a haystack." ’ _arXiv preprint arXiv:2309.15807_ **Examples** (2023). 

## ~~**Latent**~~ **diffusion models** 

## Deep Denoising Probabilistic Model (DDPM) are the first step towards diffusion models: 

- **forward diffusion** gradually perturbs the input data. 

- **reverse denoising** process that learns to generate data through iterative denoising 

## **forward diffusion** 

**reverse denoising** 

## **Forward process** 

𝐱 𝟎 

𝐱 1 

**==> picture [44 x 18] intentionally omitted <==**

**----- Start of picture text -----**<br>
𝐱<br>t−1<br>**----- End of picture text -----**<br>


**==> picture [21 x 18] intentionally omitted <==**

**----- Start of picture text -----**<br>
𝐱<br>t<br>**----- End of picture text -----**<br>


**==> picture [24 x 17] intentionally omitted <==**

**----- Start of picture text -----**<br>
𝐱<br>L<br>**----- End of picture text -----**<br>


## **Forward process** 

## In the forward process we are gradually adding Gaussian noise over T* steps 

**==> picture [414 x 102] intentionally omitted <==**

**----- Start of picture text -----**<br>
**<br>𝐱𝟎 𝐱1 𝐱t−1<br>…<br>**----- End of picture text -----**<br>


**==> picture [225 x 84] intentionally omitted <==**

**----- Start of picture text -----**<br>
𝐱t 𝐱T<br>…<br>**----- End of picture text -----**<br>


- *L is also used in notation ** _i_ is also used in notation 

## **Forward process** 

𝐱 𝟎 

𝐱 𝐱 𝑝 1 0 

𝐱 1 … 

𝑝 𝐱t 𝐱t−i 𝑝 𝐱T 𝐱T−1 𝐱t−1 𝐱t 𝐱T … 

## **Forward process** 

𝑝 𝐱1 𝐱0 𝑝 𝐱tt 𝐱t−it−i 𝑝 𝐱TT 𝐱𝟎 𝐱1 𝐱t−1 𝐱tt … … ae ol AG iy eka Rone in nls a 5 2 2 𝐱 𝐱 ≔𝒩 𝐱 𝐱 𝐈 𝑝 t t−𝟏 t; 1 −𝛽t t−𝟏, 𝛽t ( ~~SS )~~ 2 **Mean Variance (depends only on** 𝛽t **!)** 

𝑝 𝐱tt 𝐱t−it−i 𝑝 𝐱TT 𝐱T−1 𝐱tt 𝐱T … 

Gaussian perturbation step 

## **Forward process** 

𝑝 𝐱1 𝐱0 𝑝 𝐱t 𝐱t−i 𝑝 𝐱T 𝐱T−1 𝐱𝟎 𝐱1 𝐱t−1 𝐱t 𝐱T us eta She aati a a aec Peed … … 

## This is mathematical equivalent to sampling as follows 

𝐱 = 𝛼 𝝐 ∼𝒩 t t 𝐱t−𝟏 + 𝛽t 𝝐t, 𝒊 𝟎, 𝐈i.i.d. 

At each step t, we scale down the previous state scaled by This is the same reparametrization trick from VAE! 𝛼t and add a controlled amount of Gaussian noise scaled by 𝛽t 

## **Reverse process** 

Parameterized (learnable) function with weights 𝜙 learns to denoise 

**==> picture [809 x 213] intentionally omitted <==**

**----- Start of picture text -----**<br>
𝐱𝟎 𝐱1 𝐱t−1 𝐱t 𝐱T<br>… …<br>𝑝𝜙 𝐱0 𝐱1 𝑝𝜙 𝐱t−1 𝐱t 𝑝𝜙 𝐱T 𝐱T−1<br>**----- End of picture text -----**<br>


Our **goal** is to learn this function 

## **DDPM from a variational perspective** 

**==> picture [407 x 54] intentionally omitted <==**

**----- Start of picture text -----**<br>
Decoder<br>Encoder<br>z<br>𝜃 𝜙<br>**----- End of picture text -----**<br>


## VAE: trainable encoder and decoder 

… … 

DDPM: fixed encoder - trainable decoder – hierarchical process 

## **The forward process: sampling at time t** 

The noise at the time T can be computed by applying the chain: T 𝐱 𝐱 𝐱 𝐱 𝑝 ( 𝟏:T |) 𝟎 𝑝 (|) 𝐭 𝐭−𝟏 = ෑ 𝑡=1 If we assume a Gaussian transition kernel 2 2 𝐱 𝐱 ≔𝒩 𝐱 𝐱 𝐈 𝑝 t t−𝟏 t; 1 −𝛽t t−𝟏, 𝛽t cry (7) we can compute a closed form: 2 𝑝t (|!) 𝐱t 𝐱𝟎 = 𝒩 (€ 𝐱t; 𝛼 7 𝑡, 𝐱𝟎, € 1 − 𝛼 )) 𝑡 𝐈 with ഥ𝛼 𝑡 2 𝑡 𝛼 t ≔ςk=1 1 −𝛽𝑘 = ς𝑘=1 𝑘 

## **Step-by-step recursion** 

𝔼 𝐱 𝐱 = 𝛼 𝐱 + 𝜎 𝔼 𝝐 = 𝛼 𝐱 𝟏 𝟎 1 𝟎 1 𝟏 1 𝟎 SS —_—S’ = 𝟎 

𝔼 𝐱𝟐 𝐱𝟎 = 𝛼2, 𝔼 𝐱𝟏 𝐱𝟎 + 𝜎2 𝔼 𝝐𝟐 = 𝛼2𝛼1𝐱𝟎 >’ =𝟎 

𝔼 𝐱 𝐱 = 𝛼 𝐱 𝐱 = 𝛼 𝛼 𝛼 𝐱 𝟑 𝟎 3, 𝔼 𝟐 𝟎 3 2 1 𝟎 

Recall: the expected value of a random variable is its **weighted average** , where each possible value is weighted by its probability of occurring. It represents the "center of mass" of the probability distribution. For a Gaussian distribution, it corresponds to the mean 

**==> picture [357 x 78] intentionally omitted <==**

**----- Start of picture text -----**<br>
𝑡<br>=<br>𝔼 𝐱𝐭 𝐱𝟎 𝛼𝑡, 𝐱𝟎, 𝛼𝑡 𝛼𝑘<br>≔ෑ<br>𝑘=1<br>**----- End of picture text -----**<br>


## **Sampling at time t** 

## The closed form 

2 𝑝t 𝐱t 𝐱𝟎 = 𝒩 𝐱t; 𝛼𝑡, 𝐱𝟎, 1 − 𝛼𝑡 𝐈 

## can be rewritten as 

2 𝐱𝑡 = 𝛼𝑡𝐱𝟎 + 1 − 𝛼𝑡 𝝐, 𝝐∼𝒩 𝟎, 𝐈 

**Take home point #1** We do not need to iteratively add noise, we can directly sample the noisy image at each step of the forward process 

## **Noise schedule** 

- Classic DDPM default (Ho et al., 2020). 

- The image is visually recognisable at low t; by t ≈ 0.5– 0.7 most signal is gone. 

- _Problem: destroys signal too fast — many steps wasted in the 'all noise' regime._ 

- Proposed by Nichol & Dhariwal (2021) for improved quality. 

- Keeps more semantic structure at mid-noise levels → better gradient signal. 

- _Key intuition: visual content degrades gradually; the model can learn fine details at low noise._ 

## **Sampling at time t** 

## The closed form 

2 𝑝t 𝐱t 𝐱𝟎 = 𝒩 𝐱t; 𝛼𝑡𝐱𝟎, 1 − 𝛼𝑡 𝐈 can be rewritten as 2 𝐱𝑡 = 𝛼𝑡𝐱𝟎 + 1 − 𝛼𝑡 𝝐, 𝝐∼𝒩 𝟎, 𝐈 **Take home point #2** The sequence {βt} is a pre-determined, monotonically increasing noise schedule, βt ∈(0,1). As 𝑇→∞ and the noise schedule increases: 𝑝𝑇 𝐱𝑇 𝐱𝟎 ⟶𝒩 𝟎, 𝐈 as 𝑇→∞ 

can be rewritten as 

## **The reverse process** 

Our goal is to approximate the true (unknown) reverse transition q 𝔼 𝐷 𝐱 𝐱 𝐱 𝐱 pt col 𝐱t KL €C q t−𝟏 1) t ฮ𝑝𝜙 € t−𝟏 1 t YY’ This function is intractable, but we can make it tractable by conditioning on a 𝐱 . clean data sample 𝟎 

𝑞 𝐱t 𝐱t−𝟏 𝑞 𝐱t−𝟏 𝐱𝟎 𝑞 ( 𝐱t−𝟏 𝐱t, 𝐱𝟎 = ( | ) ¢ |) 𝐱 𝐱 | ) 𝑞 CT.) t 𝟎 This formulation is tractable and admits a closed form analytical solution 

## **Modeling the reverse transition** 

**Assumption** : the reverse transition is Gaussian 

- 2 

- 𝐱 𝐱 ≔𝒩 𝐱 𝐈 

- 𝑝ϕ t-1 t t-1; μϕ(𝐱t, t), σt 

## **Mean** 

- Parametric function 𝜇 ⋅, t : ℝ[D] →ℝ[D] 𝜙 

- Computes the “mean” image 

**Variance** 

   - fixed 

   - function of ഥ𝛼 and 𝑡 βt 

- Takes as input the previous image and the time step 

## **Computing the reverse transition** 

Diffusion model often use U-net architectures with residual blocks, skipconnections and self-attention layers. 

The time step is given as input (using, e.g., sinusoidal encoding) → the reverse function depends on the step! 

## **Computing the reverse transition** 

Diffusion model often use U-net architectures with residual blocks, skipconnections and self-attention layers. 

## **Computing the reverse transition** 

Diffusion model often use U-net architectures with residual blocks, skipconnections and self-attention layers. 

- Why U-Net works well for denoising: • multi-scale feature fusion 

- • respects spatial structure 

Other architectures (e.g. Transformer based) are possible 

## **Computing the reverse transition** 

Diffusion model often use U-net architectures with residual blocks, skipconnections and self-attention layers. 

Bottleneck 

- × 

- Lowest resolution (8 8) — self-attention for global coherence. 

## **Training the reverse transition** 

**Recall** : the reverse transition is Gaussian 

2 𝑝ϕ ( 𝐱t-1 |) 𝐱t ≔𝒩 ( 𝐱t-1; μϕ(𝐱t, t), σt 𝐈 

Given a training sample 𝐱𝟎, we want 

𝑝ϕ 𝐱t−1 𝐱t to be as close as possible to 𝑞 ( 𝐱t−𝟏 | 𝐱t, 𝐱𝟎 ) 

Intuition: if the learned reverse process is supposed to subtract away the noise, then whenever we’re working with a specific 𝐱𝟎 it should subtract it away exactly as exact reverse process would have. 

Learn to approximate the mean μϕ(𝐱t, t) from μq(𝐱t, 𝐱0) 

T 1 2 ℒdiffusion () 𝐱𝟎; 𝜙= ෍ 2σ ~~—|~~ 2 μϕ(𝐱t, t)-μq(𝐱t, 𝐱0, t) | 2 + 𝐶 t t=1 

Averaging over the data distribution and omitting 𝐶, the final DDPM training objective is: 

T 1 2 ℒDDPM () 𝜙= ෍ 2σ ~~—~~ 2[𝔼][𝐱] 𝟎[𝔼] p (1 𝐱t 𝐱𝟎 lll μϕ(𝐱t, t) −μq(𝐱t, 𝐱0, t) I 2 | t t=1 

## **Training the reverse transition** 

**Recall** : the reverse transition is Gaussian 

2 𝑝ϕ 𝐱t-1 𝐱t ≔𝒩 𝐱t-1; μϕ(𝐱t, t), σt 𝐈 

Given a training sample 𝐱𝟎, we want 

In practice, either: 

𝑝ϕ 𝐱t−1 𝐱t to be as close as possible to 𝑞 𝐱t−𝟏 𝐱t, 𝐱𝟎 

2 2 μϕ(𝐱t, t) −μq(𝐱t, 𝐱0, t) 2 ∝ 𝐱𝜙(𝐱t, t) −𝐱𝟎 2, 𝐱𝟎 ∼𝑝data 

Unet𝜙(𝐱t, t) = 𝐱𝜙 (𝐱t, t) predicts denoised image at step t 

Intuition: if the learned reverse process is supposed to subtract away the noise, then whenever we’re working with a specific 𝐱𝟎 it should subtract it away exactly as exact reverse process would have. 

## **Training the reverse transition** 

**Recall** : the reverse transition is Gaussian 

2 𝑝ϕ 𝐱t-1 𝐱t ≔𝒩 𝐱t-1; μϕ(𝐱t, t), σt 𝐈 

Given a training sample 𝐱𝟎, we want 

In practice, either: 

𝑝ϕ 𝐱t−1 𝐱t to be as close as possible to 𝑞 𝐱t−𝟏 𝐱t, 𝐱𝟎 

2 2 μϕ(𝐱t, t) −μq(𝐱t, 𝐱0, t) 2 ∝ 𝐱𝜙(𝐱t, t) −𝐱𝟎 2, 𝐱𝟎 ∼𝑝data 

Unet𝜙(𝐱t, t) = 𝐱𝜙 (𝐱t, t) predicts denoised image at step t 

or: 

Intuition: if the learned reverse process is supposed to subtract away the noise, then whenever we’re working with a specific 𝐱𝟎 it should subtract it away exactly as exact reverse process would have. 

2 2 μϕ(𝐱t, t) −μq(𝐱t, 𝐱0, t) 2 ∝ 𝝐𝜙(𝐱t, t) −𝝐 2, 𝝐 ∼𝑝noise Unet𝜙(𝐱t, t) = 𝝐𝜙 (𝐱t, t) predicts **noise** added during training 

## **Training the reverse transition** 

**Recall** : the reverse transition is Gaussian 

2 𝑝ϕ 𝐱t-1 𝐱t ≔𝒩 𝐱t-1; μϕ(𝐱t, t), σt 𝐈 

Given a training sample 𝐱𝟎, we want 

In practice, either: 

𝑝ϕ 𝐱t−1 𝐱t to be as close as possible to 𝑞 𝐱t−𝟏 𝐱t, 𝐱𝟎 

2 2 μϕ(𝐱t, t) −μq(𝐱t, 𝐱0, t) 2 ∝ 𝐱𝜙(𝐱t, t) −𝐱𝟎 2, 𝐱𝟎 ∼𝑝data 

Unet𝜙(𝐱t, t) = 𝐱𝜙 (𝐱t, t) predicts denoised image at step t 

or: 

Intuition: if the learned reverse process is supposed to subtract away the noise, then whenever we’re working with a specific 𝐱𝟎 it should subtract it away exactly as exact reverse process would have. 

2 2 μϕ(𝐱t, t) −μq(𝐱t, 𝐱0, t) 2 ∝ 𝝐𝜙(𝐱t, t) −𝝐 2, 𝝐 ∼𝑝noise Unet𝜙(𝐱t, t) = 𝝐𝜙 (𝐱t, t) predicts **noise** added during training **→ empirically the best option** 

## **Training loop in practice** 

For each batch – until convergence 

1. Sample noise to add to each image in the batch 

2. Sample a random timestep for each image  t 

3. Add noise to the clean images according to the noise magnitude 

4. Predict the noise residual 

5. Compute the MSE loss 6. Update the gradients 

No need to compute the entire chain for each image – we can predict the noisy image at an arbitrary time step 

Image source: Ho et al. 2020 

## **At inference time** 

## Predict the noise and subtract it 

Use the new image as input to the denoising autoencoder 

Source: https://learnopencv.com 

# But why latent diffusion models? 

## **Problems with pixel-space diffusion** 

How much does it cost to sample an image 1024 × 1024 × 1000 timesteps? 

## **Problems with pixel-space diffusion** 

How much does it cost to sample an image 1024 × 1024 × 1000 timesteps? 

- Computational cost **scales with resolution** 𝒪(H × W) per each timestep t 

- DDPM originally worked at 32 × 32 or 64 × 64 — scaling to high resolution is extremely expensive. 

- Each denoising step requires a **full forward pass** of a large U-Net over the full spatial resolution. 

- 1000 sequential steps: real-time generation at high-res is infeasible on a single GPU 

# Realism = structure 

# Realism = structure + detail 

## **Semantic compression** 

𝐱 T 

**==> picture [21 x 18] intentionally omitted <==**

**----- Start of picture text -----**<br>
𝐱<br>t<br>**----- End of picture text -----**<br>


**==> picture [24 x 17] intentionally omitted <==**

**----- Start of picture text -----**<br>
𝐱<br>𝟎<br>**----- End of picture text -----**<br>


## **Idea: decouple semantics from detail** 

## **Latent diffusion models** 

The LDM idea (Rombach et al., 2022): first compress image to latent _z_ with a perceptual VAE, then run diffusion entirely in latent space 

**==> picture [580 x 90] intentionally omitted <==**

**----- Start of picture text -----**<br>
𝐱 z ො𝐱<br>Encoder Decoder<br>ℰ 𝒟<br>**----- End of picture text -----**<br>


## **Latent diffusion models** 

The LDM idea (Rombach et al., 2022): first compress image to latent _z_ with a perceptual VAE, then run diffusion entirely in latent space 

**==> picture [810 x 252] intentionally omitted <==**

**----- Start of picture text -----**<br>
𝐱 z ො𝐱<br>Train VAE<br>Encoder Decoder ො𝐱= 𝒟 ℰ 𝐱<br>Pixel-wise or<br>ℰ 𝒟<br>feature-wise<br>reconstruction<br>loss<br>as PC as PC as PC as PC as PC as PC as PC as PC as PC as PC as Le<br>se acer<br>Sere<br>ae Train denoising U-net<br>' ' in latent space<br>**----- End of picture text -----**<br>


## **From pixel to latent space** 

- Two-stage training: original model 

      - Stage 1 — Train a perceptual autoencoder to compress images to z and reconstruct faithfully. 

      - Stage 2 — Train a DDPM entirely in the low-dimensional latent space z. 

- One-stage training: possible in more recent models 

- At inference: sample ෤𝐳 the diffusion model, then decode ෤𝐳→𝐱 via 𝒟. 

- The two stages are decoupled — the diffusion model never sees raw pixels. 

   - **Reduce computational load: 64 × 64 latent vs. 512 × 512 pixel → 64 × fewer operations per diffusion step!** 

## **From pixel to latent space** 

- Two-stage training: original model 

   - Stage 1 — Train a perceptual autoencoder to compress images to z and reconstruct faithfully. 

   - Stage 2 — Train a DDPM entirely in the low-dimensional latent space z. 

- One-stage training: possible in more recent models 

- At inference: sample ෤𝐳 the diffusion model, then decode ෤𝐳→𝐱 via 𝒟. 

- The two stages are decoupled — the diffusion model never sees raw pixels. 

**Flexible method: change domain by changing the VAE block** 

## **The VAE component** 

Adversarial discriminator (improve fidelity) 

**==> picture [681 x 261] intentionally omitted <==**

**----- Start of picture text -----**<br>
𝐱 z ො𝐱<br>Encoder Decoder<br>ℰ 𝒟<br>Regularize latent space<br>1) Use KL-divergence w.r.t. to prior (as in regular VAE)<br>2) Discretize encoding as in VQ-VAE<br>**----- End of picture text -----**<br>


**Real Fake** 

# Putting everything together 

A decadent chocolate cake adorned with decorative sugar art 

An emu wearing sun glasses and chilling on a beach 

Sunset in a valley with trees and mountains 

A beaver dressed in a f i ‘ i. F = 7 = ~ vest, wearing glasses C A painting of an adorable rabbit An old man with green and a vibrant necktie, \ a sitting on a colourful splash eyes and a beard in a library 

A traditional tea house in a tranquil garden with blooming cherry 

## **Latent diffusion model** 

Rombach et al. "High-resolution image synthesis with latent diffusion models." _CVPR_ 2022. 

## **Towards conditional synthesis** 

Unconditional synthesis 

2 ℒ = 𝔼 𝝐 LDM (𝒛t, t) −𝝐 ℰ(x),ϵ,t 𝜙 2 2 = ℒLDM 𝔼 𝝐 (𝒛t, τθ(y), t) −𝝐 ℰ(x),ϵ,y,t 𝜙 2 ~~ST~~ **Domain-specific expert** Co-trained with the LDM 

Conditional synthesis 

## **Conditional generation** 

**Cross-Attention Mechanism (For Text/Class Inputs):** the text is converted into embeddings (vectors) via a language model (like CLIP) and injected into the denoising U-Net through cross-attention layers. **Concatenation Mechanism (For Spatial Inputs):** When the input is spatially aligned—such as image-to-image translation, inpainting, or semantic maps— the switch directs the input to be concatenated directly to the noisy latent input of the U-Net 

## **Semantic synthesis** 

## **Training at scale: Exponential Moving Average** 

**Latent diffusion models are heavy models:** 

- Small batch sizes create instabilities during training 

- **During training, two copies of the U-Net are maintained in parallel:** • 𝜃 (optimizer weights): updated at every gradient step via Adam — noisy, can fluctuate. 

- 𝜃 EMA (EMA weights): updated as a running average — smooth, 

- stable trajectory through parameter space. 

EMA𝑡 = 𝛼⋅𝑥𝑡 + 1 −𝛼⋅EMA𝑡−1, 𝛼∈ 0,1 

## **Why diffusion models won** 

- Progressive denoising yields sharp, high-fidelity images. 

- Early steps determine global structure; later steps add fine detail. 

- Stochastic reverse process explores the full data distribution. 

- No mode collapse unlike GANs. 

- Fixed encoder — only the denoising decoder is learned. 

- Well-conditioned denoising subproblems at each noise level. 

## **Major generative model families** 

## **Autoregressive Models** 

_GPT, PixelCNN, WaveNet_ 

- **Computes exact likelihood + Multi-modal, flexible model - Slow inference (token-by-token) - Depends on tokenization** 

## **Variational Autoencoders** 

_VAE, β-VAE, VQVAE_ 

**Explicit latent space modelling May serve as building blocks for other models** 

**+ Interpretability of the feature space + Fast inference** 

## **GANs** 

_GAN, StyleGAN, BigGAN_ 

**Implicit distribution + Fast inference (single step) + Photo-realism** 

- **Unstable training** 

**+ Effective representation learning - Blurry images, low realism** 

**Normalizing Flows** _Glow, RealNVP, MAF_ 

**Diffusion Models** _DDPM, Stable Diffusion, DALL-E_ 

**Energy-Based Models** _EBM, NCSN, ScoreNet_ 

**Exact inference** 

**State of the art Denoising in latent space + High quality image synthesis + Flexible and highly controllable synthesis - Slow inference - Requires massive pretraining** 

**Flexible density** 

## **Bibliography** 

https://the-principles-of-diffusion-models.github.io/ See chapters 1, 2, 8, 9 Neurips 2023 Tutorial Latent diffusion models: is the generative AI revolution happening in latent space? Denoising diffusion probabilistic models https://arxiv.org/abs/2006.11239 High-Resolution Image Synthesis with Latent Diffusion Models https://arxiv.org/abs/2112.10752 Emu: Enhancing Image Generation Models Using Photogenic Needles in a Haystack https://arxiv.org/abs/2309.15807 Classifier-Free Diffusion Guidance https://arxiv.org/abs/2207.12598 Adding Conditional Control to Text-to-Image Diffusion Models https://arxiv.org/abs/2302.05543 

